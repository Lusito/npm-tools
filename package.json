{
  "private": true,
  "workspaces": [
    "packages/require-libs",
    "packages/tsx-docs"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "build:tsx-docs-demo": "yarn workspace tsx-docs build:demo",
    "lint": "yarn lint:es && yarn lint:scss && yarn lint:package && yarn lint:style",
    "lint:es": "eslint \"packages/*/src/**/*.{ts,tsx}\" --ext \".ts,.tsx\" --ignore-path .prettierignore",
    "lint:es:fix": "yarn lint:es --fix",
    "lint:fix": "yarn lint:es:fix && yarn lint:scss:fix && yarn lint:package:fix && yarn lint:style:fix",
    "lint:package": "yarn lint:package:fix --check",
    "lint:package:fix": "sort-package-json package.json \"packages/*/package.json\"",
    "lint:scss": "stylelint --ignore-path .prettierignore \"packages/*/src/**/*.scss\"",
    "lint:scss:fix": "yarn lint:scss --fix",
    "lint:style": "yarn lint:style:base --check",
    "lint:style:base": "prettier \"packages/*/src/**/*.{ts,tsx,js,json,scss}\" \"packages/*/*.{ts,tsx,js,json,scss}\"",
    "lint:style:fix": "yarn lint:style:base --write",
    "release": "ts-node tools/release.ts",
    "start": "yarn workspace tsx-docs start"
  },
  "devDependencies": {
    "@lusito/eslint-config-react": "^2.0.3",
    "@lusito/prettier-config": "^2.0.0",
    "@lusito/stylelint-config": "^2.0.2",
    "@types/prompts": "^2.4.1",
    "prompts": "^2.4.2",
    "sort-package-json": "^1.57.0",
    "ts-node": "^10.9.1",
    "type-fest": "^3.2.0"
  }
}
